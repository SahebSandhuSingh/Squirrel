"""Activity matching: people who do the same activities are suggested to each other as possible
workout partners (two runners, two lifters), ranked by how much both of them care about it.

Separate from Partner Hunt: no XP gate, no location and no meeting preferences. It reads only the
matching view of a profile (features.py), which holds coarse, consented data.
"""
